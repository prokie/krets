[package]
name = "krets-parser"
version = "0.1.1"
edition = "2024"

[dependencies]
thiserror = { workspace = true }
faer = { workspace = true }
nom = "8.0.0"
serde = { version = "1.0.228", features = ["derive"] }
toml = "0.9.8"
